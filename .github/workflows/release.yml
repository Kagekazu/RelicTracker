name: Release

on:
  push:
    tags:
      - "v*"

permissions:
  contents: write

jobs:
  build-release:
    runs-on: windows-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: "10.0.x"

      - name: Fetch Dalamud SDK libs (api15)
        shell: pwsh
        run: |
          $ErrorActionPreference = "Stop"
          $url = "https://raw.githubusercontent.com/goatcorp/dalamud-distrib/main/api15/latest.zip"
          $tmp = Join-Path $env:RUNNER_TEMP "dalamud"
          $zip = Join-Path $tmp "dalamud.zip"
          $dst = Join-Path $tmp "extracted"
          New-Item -ItemType Directory -Force $dst | Out-Null
          Invoke-WebRequest -Uri $url -OutFile $zip
          Expand-Archive -Path $zip -DestinationPath $dst -Force

          $dalamudDll = Get-ChildItem -Path $dst -Recurse -Filter "Dalamud.dll" | Select-Object -First 1
          if (-not $dalamudDll) { throw "Dalamud.dll not found after extracting $url" }

          $env:DALAMUD_HOME = $dalamudDll.Directory.FullName
          "DALAMUD_HOME=$($env:DALAMUD_HOME)" | Out-File -FilePath $env:GITHUB_ENV -Append -Encoding utf8

      - name: Restore
        run: dotnet restore

      - name: Build (Release | x64)
        run: dotnet build RelicTracker.slnx -c Release -p:Platform=x64 --no-restore

      - name: Prepare release zip
        shell: pwsh
        run: |
          $zip = "RelicTracker\\bin\\x64\\Release\\RelicTracker\\latest.zip"
          if (!(Test-Path $zip)) { throw "Expected pack zip not found: $zip" }
          Copy-Item $zip "RelicTracker.zip" -Force

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          files: |
            RelicTracker.zip
